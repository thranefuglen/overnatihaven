# EKSEMPEL-KONFIGURATIONSFIL — kopiér til .env og erstat alle CHANGE_ME-værdier
# Denne fil MÅ committes til git. Læg aldrig rigtige hemmeligheder her.

# Server Configuration
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL=CHANGE_ME

# Email Configuration (valgfrit - til at sende notifikationer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=CHANGE_ME
EMAIL_FROM=your-email@example.com
EMAIL_TO=recipient@example.com

# CORS (tillad frontend domæne)
CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10

# JWT Configuration
JWT_SECRET=CHANGE_ME
JWT_EXPIRES_IN=24h

# File Upload Configuration
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/webp

# Vercel Blob Storage (bruges til billede-uploads i produktion)
BLOB_READ_WRITE_TOKEN=CHANGE_ME

# Vite (frontend)
VITE_API_URL=http://localhost:3000/api
